# MN-Core 2 レジスタ/メモリセット・命令セット整理

https://projects.preferred.jp/mn-core/assets/mncore2_dev_manual_ja.pdf

## 1. この文書の目的と出典

この文書は、MN-Core 2 のアセンブラおよびインタプリタを実装するために必要な低レベル仕様を、日本語で単独参照しやすい形に再構成したものである。主な出典は次の 3 つである。

- `mncore2_dev_manual_ja.pdf` の第 1 章、第 3 章、第 4 章
- 公式 Web ページ `https://projects.preferred.jp/mn-core/`
- 同梱の `mncore2_emuenv_20240826/README.md` と `mncore2_emuenv_20240826/tutorial.md`

特に公式 Web ページからは、次のアーキテクチャ上の位置づけを補っている。

- MN-Core 2 は fully-deterministic な完全 SIMD 系アクセラレータである
- 各 PE は独立したプログラムカウンタや命令デコーダを持たず、ホストが生成した命令列を完全同期で実行する
- `MAU + 4PE` を 1 つの MAB とし、MAB を階層化されたメモリ木にぶら下げる構造を取る
- MN-Core 2 の公開カタログスペックは FP64 `12 TFLOPS`、FP32 `49 TFLOPS`、TF32 `98 TFLOPS`、TF16 `393 TFLOPS`

この文書は命令の意味とオペランド規則を重視しており、加えて第 4 章に基づく数値演算のビット正確意味論もまとめる。ホスト API やボード制御の詳細は必要最小限に留める。

## 2. 全体アーキテクチャ

### 2.1 階層構造

MN-Core 2 の計算資源は概ね次のように階層化される。

- 4 グループ
- 各グループに 2 個の L2B
- 各 L2B に 8 個の L1B
- 各 L1B に 16 個の MAB
- 各 MAB に 4 個の PE と 1 個の MAU

したがって総 PE 数は `4 x 2 x 8 x 16 x 4 = 4096` である。

### 2.2 実行モデル

- MV 命令は 4 ステップに 1 回実行される上位メモリ転送命令である
- PE 命令は VLIW 形式で、1 ステップ内で複数の命令式を同時発行できる
- PE は完全同期で動作するため、条件分岐よりもマスクとデータ転送で制御する
- ハザード回避や待機はソフトウェア責務であり、必要な待ちを明示する必要がある

### 2.3 語長とアクセス単位

- 半語: 16 bit
- 単語: 32 bit
- 長語: 64 bit
- 2 長語: 128 bit


文法上の `l` は長語、`ll` は 2 長語を表す。LM/GRF のアドレスは単語単位、PDM/DRAM/L2BM/L1BM のアドレスは長語単位、行列レジスタは行または列単位で数える。

### 2.4 メモリの各種類

MN-Core 2 で扱う主なメモリ種別とその特徴は以下の通り。

- **PDM (Parameter Data Memory)**: グループごとに存在する上位メモリ。容量は 4 MiB/グループ、アドレス単位は長語。主に DRAM や L2BM との間で MV 転送に用いられる。

- **DRAM**: グループごとの大容量メインメモリ。容量は 4 GiB/グループ、アドレス単位は長語。ホストとのデータ受け渡しや大規模テンソルの保持に使われる。

- **DAR (DRAM Address Register)**: DRAM 間接参照用のアドレステーブル。各グループに 1024 エントリ。MV 命令で DRAM の間接アクセス時に利用される。

- **L2BM (Level 2 Broadcast Memory)**: 各 L2B に付属する中間メモリ。容量は 32 Ki 長語/L2BM。グループ内外のデータ放送や縮約の中継点となる。

- **L1BM (Level 1 Broadcast Memory)**: 各 L1B に付属するローカルブロードキャストメモリ。容量は 8 Ki 長語/L1BM。MAB 単位の転送や縮約、PE 群への放送に使われる。

- **LM0/LM1 (Local Memory 0/1)**: 各 PE 内のローカルメモリ。各 2 Ki 長語/PE、アドレス単位は単語。主作業メモリとして利用され、LM0 は T レジスタ間接参照が可能。

- **GRF0/GRF1 (General Register File 0/1)**: 各 PE 内の汎用レジスタファイル。各 256 長語/PE、アドレス単位は単語。ALU/MAU の主要な入出力先。

- **T レジスタ**: 各 PE 内のテンポラリレジスタ。8 長語/PE (2 長語 x 4 サイクル)。LM0 の間接参照や一時値保持に使われる。

- **行列レジスタ (x/y)**: 各 MAB に 2 面 (x, y)。物理的には 16 行/面。MAU の行列演算や転置読み出しに利用される。

これらのメモリは、命令種別やオペランド構文ごとにアクセス単位やアドレス指定方法が異なる。詳細は第 4 章以降の各メモリ節を参照。

### 2.5 MAB の浮動小数点・行列演算能力

manual 第 1.4 節の要点として、1 つの MAB は `4 PE + 1 MAU` を持ち、浮動小数点の高密度演算は主に MAU が担う。
MAU には次の 2 つの実行モードがある。

- 行列ベクトル積和演算モード: $y = A \times b + c$
- ベクトル積和演算モード: $y = a \times b + c$ を要素ごとに独立実行

#### 2.5.1 行列ベクトル積和モード

1 個の MAU は 1 サイクルで、次のいずれかの行列ベクトル積和を実行できる。

- 倍精度: `m = 2`, `n = 4`
  - `A`, `b`: ブロックフロート倍精度
  - `c`, `y`: 通常の倍精度
  - 1 MAB あたり `2 x 4 = 8` FMA/cycle、すなわち `16 FLOP/cycle`
- 単精度: `m = 8`, `n = 4`
  - `A`, `b`: ブロックフロート単精度
  - `c`, `y`: 通常の単精度
  - 1 MAB あたり `8 x 4 = 32` FMA/cycle、すなわち `64 FLOP/cycle`
- 疑似単精度: `m = 8`, `n = 8`
  - `A`, `b`: ブロックフロート疑似単精度
  - `c`, `y`: 通常の単精度
  - 1 MAB あたり `8 x 8 = 64` FMA/cycle、すなわち `128 FLOP/cycle`
- 半精度: `m = 16`, `n = 16`
  - `A`, `b`: ブロックフロート半精度
  - `c`, `y`: 通常の単精度
  - 1 MAB あたり `16 x 16 = 256` FMA/cycle、すなわち `512 FLOP/cycle`

manual はボード全体の倍精度ピーク値を `2 x 4 x 2 x 1024 x f` と書いている。ここで:

- 最初の `2 x 4` は 1 MAU が 1 サイクルで処理する倍精度行列サイズ
- 次の `2` は FMA 1 回を `2 FLOP` と数えるための係数
- `1024` は 1 ボード上の MAB 数
- `f` は動作周波数

したがって、行列モードのピーク FLOPS は「1 MAB あたりの FMA 数」をそのまま 1024 倍してボード値へ上げればよい。

#### 2.5.2 ベクトル積和モード

ベクトルモードでは、1 個の MAU は 1 サイクルで次のいずれかを実行できる。

- 倍精度: `m = 4`
  - `a`, `b`, `c`, `y` は通常の倍精度
  - ただし積が有効なのは `(0,1)` 側または `(2,3)` 側のどちらかだけで、1 回の実行で有効なのは 2 要素分
  - 4 要素全体の FMA を完了するには 2 回実行し、2 回目の `c` に 1 回目の結果を渡す
  - 1 MAB あたり実効 `2` FMA/cycle、すなわち `4 FLOP/cycle`
- 単精度: `m = 8`
  - `a`, `b`, `c`, `y` は通常の単精度
  - 1 MAB あたり `8` FMA/cycle、すなわち `16 FLOP/cycle`
- 半精度: `m = 16`
  - `a`, `b`: 通常の半精度
  - `c`, `y`: 通常の単精度
  - 1 MAB あたり `16` FMA/cycle、すなわち `32 FLOP/cycle`

manual はベクトルモードの倍精度ピーク値を `2 x 2 x 1024 x f` と書いている。これは「1 回の実行で有効な倍精度 FMA が 2 要素分である」という制約をそのまま反映した式である。

また manual は、`m = 8` の単精度 FMA がすでに可能なので、ベクトル積和モードには疑似単精度に相当する独立モードは存在しないと明記している。

#### 2.5.3 行列レジスタが保持する行列サイズ

MAB の行列演算能力は、そのまま行列レジスタの物理サイズにも対応している。

- 倍精度行列: `4 x 4`
  - `dmwrite` / `dmread` 1 命令で全体を扱える
- 単精度・疑似単精度行列: `8 x 8`
  - `fmwrite` / `gmwrite` / `fmread` / `gmread` は 2 命令で全体を扱う
- 半精度行列: `16 x 16`
  - `hmwrite` / `hmread` は 2 命令で全体を扱う

実装上は、MAU 命令の `m x n` 能力と行列レジスタの論理サイズを分けて考えると整理しやすい。特に:

- 倍精度 matvec は `4 x 4` 行列全体のうち、1 命令で出るのは 2 行分
- 単精度 matvec は `8 x 8` 行列のうち、1 命令で使うのは 8 行 x 4 列ぶん
- 半精度 matvec は `16 x 16` 行列全体を 1 命令で使える

そのため、命令仕様を見ると単精度だけ `8 x 4` に見えるが、これは「1 命令あたりの有効列数」の話であり、行列レジスタの論理サイズ自体は `8 x 8` である。

## 3. 実装前提になる共通ルール

### 3.1 命令文の種類

- コメント文: `#` で始まる行。翻訳時に無視される
- 制御文: `quit`, `d get`, `d set`
- MV 命令文: 1 行 1 命令。上位メモリの転送に使う
- PE 命令文: セミコロン `;` で複数命令式を同時発行できる

### 3.2 アドレスのラップアラウンド

すべてのメモリで、サイズを超えるアドレスはラップアラウンドする。実装ではアドレス検証と実行時 wrap の両方を分けて扱うとよい。

- PDM: グループあたり 4 MiB = `512 Ki` 長語
- DRAM: グループあたり 4 GiB = `512 Mi` 長語
- L2BM: `32 Ki` 長語
- L1BM: `8 Ki` 長語
- LM0/LM1: `4096` 単語
- GRF0/GRF1: `512` 単語アドレス空間 (`256` 長語)
- DAR: `1024` エントリ

### 3.3 Auto-stride モードと Flat モード

PE オペランドの多くは 2 つのアドレッシングモードを持つ。

- Auto-stride mode: 開始アドレスとサイクル間増分を指定する
- Flat mode: 4 サイクルぶんのアドレスを `[a0,a1,a2,a3]` で直接指定する

LM0 の auto-stride アドレスは、概念上次式で決まる。

```text
(BAR + (T[C] if TI else 0) + ADDR + ADRI * C * WL + (WL if MAADJ and PE <= MADPE else 0)) % 4096
```

ここで `BAR` はベースアドレスレジスタ、`TI` は T レジスタ間接参照、`WL` はアクセス語長を単語換算した値である。

### 3.4 マスクの 2 種類

- 書き込みマスク: 書き込むかどうかを制御する
- ゼロフラッシュマスク: 演算結果や転送データをゼロに潰す

マスクレジスタ 1 エントリは 4 サイクル x 4 ビット = 16 ビットであり、サイクル方向とワード方向を別々に持つ。マスクの解釈はアクセス語長によって変わる。

書き込みマスクには次の 2 つの指定方法がある。

- 複数行適用: `mask[l|ll][r][s][t][m][n][k] <addr>`
- 単一行適用: `<dst>/([ll]<mask-pattern>|$[ll]imr<adr>)[t|p]`

複数行適用は以降の PE 命令式全体に効く制御文であり、アセンブリ先頭では暗黙に `mask 0` が有効になっている。`[r][s][t][m][n][k]` は順に GRF0、GRF1、T、LM0 と LM0 BAR、LM1 と LM1 BAR、マスクレジスタへの書き込みに対してマスクを有効化する指定で、順不同に書ける。

単一行適用は個々の書き込み先オペランドにだけ付く。`<mask-pattern>` は 4 サイクルぶんの固定値パターンで、`$imr<adr>` / `$llimr<adr>` は可変エントリ 1 から 15 を読む。接尾辞 `t` / `p` は PE メモリアクセス語長とマスク適用語長の不一致を検出しやすくするための安全指定である。

### 3.5 フォワーディング

直前ステップの出力は次の読み出し専用オペランドとして参照できる。

- `mauf`: MAU 出力
- `aluf`: ALU 出力
- `lbf`: L1BM から PE への転送出力
- `mreadf`: 行列レジスタ転置読み出し出力

いずれも `nop` または `noforward` のステップでは更新されない。

### 3.6 固定値入力

ALU 第 1 入力に限り、固定値入力オペランドを使える。

- `$l2bid`: `group * 2 + l2b`
- `$l1bid`: L1B 番号
- `$mabid`: MAB 番号
- `$peid`: `mab * 4 + pe`
- `$subpeid`: MAB 内 PE 番号
- `$msb1`: MSB のみ 1、残り 0

## 4. レジスタ / メモリセット

### 4.1 PDM

- 位置: グループごとの上位メモリ
- 容量: 1 グループあたり 4 MiB
- アドレス単位: 長語
- オペランド構文: `$p<addr>` または `$p<addr>@<group>`
- 役割: PDM と DRAM/L2BM の間の MV 転送
- 実装メモ: `@<group>` 省略時は全グループの同一アドレスを意味する

### 4.2 DRAM

- 位置: グループごとのメインメモリ
- 容量: 1 グループあたり 4 GiB
- アドレス単位: 長語
- オペランド構文: `$d<addr>`, `$d<addr>@<group>`, `$di<dar_addr>`, `$di<dar_addr>@<group>`
- 役割: ホスト起点データや大規模テンソルの保持
- 実装メモ: `$di...` は DAR を使った間接参照であり、16 長語単位で次の DAR エントリに進む

### 4.3 DAR

- 正式名: DRAM Address Register
- 位置: グループごと
- 容量: 1024 エントリ
- アドレス単位: エントリ
- オペランド構文: `$dar<addr>`
- 役割: DRAM 間接参照のベースアドレス表
- 実装メモ: `l2bmdars` と `l2bmdarw` で更新する。各エントリは 32 bit の「16 長語単位アドレス」を表す

### 4.4 L2BM

- 位置: 各 L2B に付属する中間メモリ
- 容量: 1 L2BM あたり 32 Ki 長語
- アドレス単位: 長語
- オペランド構文
  - MV 命令: `$lc<addr>`, `$lc<addr>@.<l2b>`, `$lc<addr>@<group>.<l2b>`
  - PE の L2BM 命令: `$lc<addr>`
  - `l2bmdars` の入力専用: `$lc<addr>@.<l2b>`
- 役割: グループ内外の放送・分配・縮約の中継点
- 実装メモ: MV 側ではグループと L2B 指定の省略可否が重要。PE 側では命令種別に応じてアドレス増分が暗黙決定される

### 4.5 L1BM

- 位置: 各 L1B に付属するローカルブロードキャストメモリ
- 容量: 1 L1BM あたり 8 Ki 長語
- アドレス単位: 長語
- オペランド構文
  - L2BM 命令で使う場合: `$lb<addr>`
  - L1BM 命令で使う場合: `$(l|ll)b(<addr>|i)`
- 役割: PE 群への放送、MAB 単位転送、縮約、折り返し転送
- 実装メモ
  - `i` は折り返しレジスタ `lbi` を指す
  - L2BM 側ポートと L1BM 側ポートは分離されている
  - 通常の PE メモリと違い、アドレスインクリメントは命令種別に従って暗黙に決まる

### 4.6 LM0

- 位置: PE 内ローカルメモリ 0
- 容量: 1 PE あたり 2 Ki 長語
- アドレス単位: 単語
- オペランド構文
  - auto-stride: `$[(l|ll)]m(<addr>[v[<adri>]][j<madpe>]|t[<addr>[v[<adri>]]])`
  - flat: `$[(l|ll)]m(<flat_addrs>[j<madpe>]|t[<flat_addrs>])`
- 役割: PE ローカルの主作業メモリ
- 実装メモ
  - T レジスタ間接参照が使えるのは LM0 のみ
  - `j<madpe>` は MAB 内アドレス修飾
  - `t...` と `j...` は同時使用不可
  - アラインしていない実行時アドレスは切り捨てられる

### 4.7 LM0 ベースアドレスレジスタ

- 位置: 各 PE の LM0 に付属
- 書き込みオペランド: `$[l]mb`
- 役割: LM0 の最終アドレスに暗黙加算されるベースアドレス
- 実装メモ: 書き込み時は MSB 側 1 語の LSB 12 bit が使われる

### 4.8 LM1

- 位置: PE 内ローカルメモリ 1
- 容量: 1 PE あたり 2 Ki 長語
- アドレス単位: 単語
- オペランド構文
  - auto-stride: `$[(l|ll)]n<addr>[v[<adri>]][j<madpe>]`
  - flat: `$[(l|ll)]n<flat_addrs>[j<madpe>]`
- 役割: LM0 と同様のローカルメモリ
- 実装メモ: LM0 と違い T レジスタ間接参照は使えない

### 4.9 LM1 ベースアドレスレジスタ

- 位置: 各 PE の LM1 に付属
- 書き込みオペランド: `$[l]nb`
- 役割: LM1 の最終アドレスに暗黙加算されるベースアドレス
- 実装メモ: 動作は LM0 BAR と同様

### 4.10 GRF0

- 位置: PE 内汎用レジスタファイル 0
- 容量: 1 PE あたり 256 長語
- アドレス単位: 単語
- オペランド構文: `$[(l|ll)]r<addr>[v[<adri>]]` または `$[(l|ll)]r<flat_addrs>`
- 役割: ALU/MAU の主要入出力先
- 実装メモ: アドレス空間は 512 単語。長語アクセスなら 2 の倍数、2 長語アクセスなら 4 の倍数アラインが必要

### 4.11 GRF1

- 位置: PE 内汎用レジスタファイル 1
- 容量: 1 PE あたり 256 長語
- アドレス単位: 単語
- オペランド構文: `$[(l|ll)]s<addr>[v[<adri>]]` または `$[(l|ll)]s<flat_addrs>`
- 役割: GRF0 と同様
- 実装メモ: 文法・制約は GRF0 と同じ

### 4.12 T レジスタ

- 位置: PE 内テンポラリレジスタ
- 容量: 1 PE あたり 8 長語 = 2 長語 x 4 サイクル
- アドレス単位: なし
- オペランド構文: `$[(l|ll)]t`
- 役割: サイクル対応の一時値保持、LM0 の間接参照ソース
- 実装メモ
  - 文法上 `l` や `ll` は書けるが無視され、常に 2 長語アクセスになる
  - 実命令では現在サイクルに対応する 2 長語が読み書きされる

### 4.13 マスクレジスタ

- 位置: PE 内特殊レジスタ
- 容量: 32 エントリ
- 可変エントリ: 1 から 15
- 固定エントリ: 0 と 16 から 31
- アドレス単位: エントリ
- 書き込みオペランド: `$omr<addr>`
- 役割: 書き込み抑止とゼロフラッシュの制御
- 実装メモ
  - 1 エントリは 4 サイクル x 4 ビット
  - 可変エントリは ALU/MAU からしか書けない
  - アドレス 0 は all-1、16 以降は下位 4 bit をサイクル方向に展開した固定パターン

### 4.13.1 書き込みマスク適用

- 種別: PE メモリ書き込み時の付加指定
- 複数行構文: `mask[l|ll][r][s][t][m][n][k] <addr>`
- 単一行構文: `<dst>/([ll]<mask-pattern>|$[ll]imr<adr>)[t|p]`
- 適用先
  - GRF0
  - GRF1
  - T レジスタ
  - LM0 と LM0 ベースアドレスレジスタ
  - LM1 と LM1 ベースアドレスレジスタ
  - マスクレジスタ
- 実装メモ
  - 複数行構文は以降の PE 命令式に対する既定値を更新する制御文であり、PE 命令式そのものではない
  - 単一行構文はそのステップだけ複数行指定を打ち消して `<dst>` にだけ適用する
  - 固定値指定 `/1000` は 4 サイクルぶんのマスクを直接埋め込む形式で、`/ll0111` のように 2 長語指定もできる
  - 可変指定 `/$imr1` はマスクレジスタ可変エントリ 1 から 15 を参照し、`/$llimr1` とすると 2 長語マスクになる
  - 書き込みマスクとゼロフラッシュマスクは同時指定できるが、参照するマスクレジスタエントリは一致していなければならない
  - マスクレジスタ自体への書き込みにも書き込みマスクを適用できる

### 4.14 行列レジスタ `x` / `y`

- 位置: 各 MAB に 2 面
- 物理構成: 1 面あたり 16 行
- オペランド構文
  - 行列書き込み / 転置読み出し: `$(l|ll)(x|y)<addr>`
  - 行列ベクトル積和: `$l(x|y)`
- 論理サイズ
  - 倍精度: 4 x 4
  - 単精度: 8 x 8
  - 疑似単精度: 8 x 8
  - 半精度: 16 x 16
- 役割: MAU の行列入力と転置読み出し先
- 実装メモ
  - MAU の matvec ではブロックフロート化済み値を前提とする
  - 転置読み出しではビット列がそのままコピーされる
  - 半精度の `hmread` は常に 2 長語列読み出し

### 4.15 `mauf`

- 種別: 読み出し専用フォワーディングパス
- 構文: `$mauf`
- 内容: 直前ステップの MAU 出力
- 実装メモ: 常に 2 長語。`nop` / `noforward` では更新されない

### 4.16 `aluf`

- 種別: 読み出し専用フォワーディングパス
- 構文: `$aluf`
- 内容: 直前ステップの ALU 出力
- 実装メモ: 性質は `mauf` と同じで、生成元だけが異なる

### 4.17 `lbf`

- 種別: 読み出し専用フォワーディングパス
- 構文: `$lbf`
- 内容: 直前ステップの L1BM -> PE 転送出力
- 実装メモ: 性質は `mauf` と同じ

### 4.18 `mreadf`

- 種別: 読み出し専用フォワーディングパス
- 構文: `$mreadf`
- 内容: 直前ステップの行列レジスタ転置読み出し出力
- 実装メモ: ALU の第 1 入力でしか使えない

### 4.19 `nowrite`

- 種別: ダミー出力オペランド
- 構文: `$nowrite`
- 内容: 結果をフォワーディングだけに残し、どのメモリにも書き込まない
- 実装メモ: 他の出力オペランドと同時指定するとエラー

### 4.20 固定値入力オペランド

- 種別: ALU 第 1 入力専用
- 構文: `$l2bid`, `$l1bid`, `$mabid`, `$peid`, `$subpeid`, `$msb1`
- 内容: 実行位置や固定ビットパターンを入力する
- 実装メモ: ALU の精度指定に従って 2 長語ぶんの値として供給される

## 5. 制御文とデバッグ文

### 5.1 `quit`

- 種別: 制御文
- 基本構文: `quit`
- 内容: 以降のアセンブリを無視し、自身も機械語出力しない
- 実装メモ: アセンブラ段階で処理する擬似命令であり、実機命令ではない

### 5.2 `d get`

- 種別: デバッグ制御文
- 基本構文: `d get[<dtype>] <memory>[n<group>][c<l2b>][b<l1b>][m<mab>][p<pe>] <num_of_words>`
- 内容: エミュレータ実行時に指定メモリの内容をダンプする
- 実装メモ
  - 実機では使用不可
  - エミュレータはサイクルアキュレートではないため、直前命令が完了した状態を読む
  - `<dtype>` は `d`, `bd`, `f`, `bf`, `bg`, `h`, `bh` など
  - 行列レジスタは必ず `dtype` 指定が必要

### 5.3 `d set`

- 種別: デバッグ制御文
- 基本構文: `d set <memory>[n<group>][c<l2b>][b<l1b>][m<mab>][p<pe>] <num_of_words> <payload>`
- 内容: エミュレータ実行時に対象メモリへ値を書き込む
- 実装メモ
  - 実機では使用不可
  - 書き込み対象は L2BM/L1BM/GRF0/1/LM0/1/T など
  - ペイロードは 16 進整数・長語・単語・半語などの記法を取る

## 6. MV 命令セット

### 6.1 共通仕様

MV 命令の一般的なオペコード構造は次の通りである。

```text
mv<mode>/n<size>[i<tag>][nd<N>][p<priority>]
```

- `n<size>`: 転送語数。必須
- `i<tag>`: 待機用タグ。省略可
- `nd<N>`: DRAM 間接参照で 1 DAR エントリを連続使用する回数
- `p<priority>`: 優先度 0 から 3
- 複数の MV 命令を同時発行する場合は、単独命令の制約とは別の追加制約がある
- manual はその追加制約の存在を明示するが、具体的なチェック規則は別文書に譲っている

縮約系の MV 命令では `rrn_opcode` を使う。

```text
(d|f|h)fadd | (d|f|h)max | (d|f|h)min |
(l|i|s)iadd | (l|i|s)band | (l|i|s)and | (l|i|s)bor | (l|i|s)or
```

### 6.2 `mvnop`

- 種別: MV
- 基本構文: `mvnop`
- 内容: 何も転送しない MV NOP
- 実装メモ: パラメータやオペランドを持たない

### 6.3 `mvp`

- 種別: MV 個別転送
- 基本構文: `mvp/n<size>[i<tag>][nd<N>][p<priority>] <src> <dst>`
- 内容: 64 長語単位の個別転送を繰り返す
- 主な形式
  - `PDM -> DRAM`
  - `DRAM -> PDM`
  - `PDM -> L2BM`
  - `L2BM -> PDM`
  - `DRAM -> L2BM`
  - `L2BM -> DRAM`
  - `PDM -> PDM`
  - グループ指定省略による並列個別転送
- 実装メモ
  - 直接アドレス時は連続領域アクセスになる
  - DRAM 間接参照時は 16 長語ごとに DAR を参照する
  - 単位動作は各基本モードで固定され、`n<size>` はその倍数でなければならない

### 6.4 `mvb`

- 種別: MV 放送
- 基本構文: `mvb/n<size>[i<tag>][p<priority>] <src> <dst>`
- 内容: 1 ソースを複数 L2BM へ放送する
- 主な形式
  - `PDM -> L2BM` のグループ間放送
  - `DRAM -> L2BM` のグループ間放送
- 実装メモ
  - `PDM -> L2BM` 版と `DRAM -> L2BM` 版では単位動作と帯域が異なる
  - いずれも destination は複数 L2BM へ複製される

### 6.5 `mvb2`

- 種別: MV グループ内放送
- 基本構文: `mvb2/n<size>[i<tag>][p<priority>] <src> <dst>`
- 内容: グループ内で 1 つの DRAM ソースを 2 つの L2BM に放送する
- 実装メモ: グループ内の 2 L2BM を対にした放送。DRAM -> L2BM 専用

### 6.6 `mvb4`

- 種別: MV 分配放送
- 基本構文: `mvb4/n<size>[i<tag>][p<priority>] <src> <dst>`
- 内容: DRAM から読み出したデータをグループ間で分配しつつ複数 L2BM に放送する
- 実装メモ: DRAM 側と L2BM 側で実効単位が異なる。通常の単純放送ではなく再配置を伴う

### 6.7 `mvr`

- 種別: MV 縮約
- 基本構文: `mvr<rrn_opcode>/n<size>[i<tag>][nd<N>][p<priority>] <src> <dst>`
- 内容: 複数 L2BM の値を縮約して PDM または DRAM に書き込む
- 主な形式
  - `L2BM -> PDM` のグループ間縮約
  - `L2BM -> DRAM` のグループ間縮約
- 実装メモ: 縮約演算の種類は `rrn_opcode` で決まる

### 6.8 `mvr2`

- 種別: MV グループ内縮約
- 基本構文: `mvr2<rrn_opcode>/n<size>[i<tag>][p<priority>] <src> <dst>`
- 内容: 同一グループ内の 2 L2BM を縮約して DRAM または PDM に書き込む
- 実装メモ: `mvb2` と対になる縮約命令である

### 6.9 `mvr4`

- 種別: MV 結合縮約
- 基本構文: `mvr4<rrn_opcode>/n<size>[i<tag>][p<priority>] <src> <dst>`
- 内容: 複数グループ・複数 L2BM から読んだデータを結合縮約して DRAM に戻す
- 実装メモ: `mvb4` と対になる。縮約しながらグループ間再配置も行う

### 6.10 `mvd`

- 種別: MV 分配 / 結合
- 基本構文: `mvd/n<size>[i<tag>][nd<N>][p<priority>] <src> <dst>`
- 内容: 広い領域を複数の下位メモリへ分配、または逆に結合する
- 主な形式
  - `PDM -> L2BM` 分配
  - `L2BM -> PDM` 結合
  - `PDM -> DRAM` 分配
  - `DRAM -> PDM` 結合
- 実装メモ: 個別転送ではなく再レイアウトが主目的。インタプリタではデータ並びの規則を命令種別ごとに持つべきである

## 7. PE 命令セット

### 7.1 PE 命令の共通仕様

- 同一ステップ内で複数命令式を同時発行できる
- 並列実行条件、ハザード、マスク適用語長の整合性を満たす必要がある
- ハザード規則は一律ではなく、命令の組み合わせごとに `1` / `2` / `3` ステップ待ちや、同一アドレスに対する `6` / `10` サイクル待ちが定義される
- PE メモリ書き込みの完了は正確には 6 サイクルだが、実装上は LM/GRF/T/L1BM/L2BM の組み合わせごとの規則として扱う方が安全である
- 並列実行条件は opcode グループ単位で定義され、`nop` の排他性、MAU の `calc/mwrite/mread` の組み合わせ制約、同一行列レジスタの重複禁止などを満たす必要がある

#### 7.1.1 並列実行条件の詳細

manual 3.6.4 は、PE 命令式をセミコロン `;` で 1 行に並べて同一ステップ発行するための条件を定めている。これらはすべてアセンブラが静的に検査し、違反するとエラーになる。

まず、PE 命令式は次のグループに分けて数える。

| Group | 含まれる命令式 |
| --- | --- |
| `nop` | `nop` |
| `noforward` | `noforward` |
| `l2bm` | `l2bmdarw` を除くすべての L2BM 命令式 |
| `l2bmdarw` | `l2bmdarw` |
| `l1bm` | 折り返しでない L1BM 命令式 |
| `l1bm-turnaround` | 折り返しレジスタを使う L1BM 命令式 |
| `mau-calc` | `mfma`, `mmul`, `vfma`, `vmul`, `vadd`, `vpassa` |
| `mau-mwrite` | `mwrite` |
| `mau-mread` | `mread` |
| `alu` | すべての ALU 命令式 |
| `wait` | `wait` |

このグループ分けに対して、1 ステップ内では次をすべて満たさなければならない。

- 各グループから同時発行できる命令式は最大 1 つである
- `nop` がある場合、同時に存在できるのは `wait` だけである
- `mau-calc` / `mau-mwrite` / `mau-mread` の 3 グループから同時に発行できるのは最大 2 つである
- MAU 系を 2 つ同時に出す場合は精度指定が一致していなければならない
- `vfma` または `vmul` と `mwrite` を同時発行する場合、`vfma` / `vmul` の第 2 入力と `mwrite` の入力について、読み出し元 PE オペランド、入力符号反転、精度拡張、精度縮減の指定がすべて一致していなければならない
- 同じ行列レジスタオペランド `$x` または `$y` は 1 ステップ中に最大 1 回しか出現できない
- 複数命令式が同じ PE オペランドへ同時に書き込むことはできない
- 複数命令式が同じ PE オペランドを読み出す場合、全サイクルでアクセス領域が完全一致していなければならない
- この「同じ読み出し」には、書き込みマスクやゼロフラッシュマスクのためのマスクレジスタ読み出しも含まれる
- LM0 を同一ステップで読み書きする場合、読み出し側と書き込み側がアクセスする領域は全サイクルで一致していなければならない。LM1 も同様である
- `imm` を発行するステップでは LM0 を同時にアクセスできない
- ゼロフラッシュマスク適用は 1 ステップ中に最大 1 回だけである
- 同一ステップで複数のマスク適用がある場合、すべてのマスク適用語長は一致していなければならない

読み出し領域一致の条件は、単に「同じメモリ種別ならよい」という意味ではない。アドレス可能メモリでは、各サイクルごとに見て同じアドレス列を参照している必要がある。したがって、同じ GRF0 でも `v` や flat mode の指定が異なれば通常は同時発行不可になる。

さらに、これらの並列実行条件を満たしていても、3.6.3 節のハザード規則によっては同時発行または直後発行できないことがある。つまり実装では「opcode グループ条件」と「レイテンシ条件」を別段で検査した方が整理しやすい。

補足として、manual では `l2bmdars` が特殊条件下で他の `l2bm` 系と同時発行可能な場合に触れているが、条件が複雑で実用性も低いとして、並列実行条件の説明では同一グループ扱いに丸めている。

セミコロンで並べる順序は、アセンブル後の機械語には影響しない。ただし、エラーメッセージは先に書いた命令との組み合わせとして報告されることがあるので、診断の見え方には影響しうる。

manual の極端な有効例としては、次のように L2BM、L1BM、MAU、ALU、`wait` を 1 ステップへ混在させられる。

```text
noforward; l2bmb $lc256 $lb0 ; l2bmdarw; l1bmm $lbi $lr0v /$imr1; l1bmrdfadd $lr8v $lb256 ;
gmmul $lx $lm0v $ln0v /$imr1; gmwrite $ls0v $ly0 ; hrelu/$imr1 $t $t $t ; wait i01
```

この例が成立するのは、各命令が上のグループ制約、同一オペランド読書き制約、マスク語長制約をすべて同時に満たしているためである。

### 7.2 同期系命令

#### `nop`

- 種別: 同期
- 基本構文: `nop`
- 内容: 4 サイクル何もしない
- 実装メモ: `noforward` を含意する
- マスク: 入出力オペランドを持たないため、書き込みマスク・ゼロフラッシュマスクとも適用対象外

#### `noforward`

- 種別: 同期
- 基本構文: `noforward`
- 内容: フォワーディングパスと折り返しレジスタを更新しない
- 実装メモ: `nop` 以外の命令式と同時発行して「演算はするが forwarding 更新はしない」を表現できる
- マスク: 自身はマスク適用対象ではない。同行の他命令に対する書き込みマスク / ゼロフラッシュマスク指定は通常どおり有効

#### `wait`

- 種別: 同期
- 基本構文: `wait <tag>`
- 内容: 対応タグの MV 命令完了まで命令発行を停止する
- 実装メモ
  - 単独では PE 命令文になれず、他の PE 命令式と同時発行必須
  - タグ 0 はエラー
  - 後続の PE 命令だけでなく後続 MV 命令の発行も止まる
- マスク: 自身はマスク適用対象ではない。同行の他命令にマスク指定があっても `wait` 自身は影響を受けない
- 出力マスク: `nop`, `noforward`, `wait` はいずれもマスクフラグを生成しない

### 7.3 L2BM 命令

#### 共通補足

- `@<l1bset>` は L1B 部分集合指定
- `l2bmr...` 系の `<rrn_opcode>` は MV と同じ縮約演算指定を使う
- アラインメントは命令ごとの単位動作に依存する
- マスク: `l2bmb`, `l2bmb2`, `l2bmd`, `l2bm@<l1badr>`, `l2bmr<rrn_opcode>`, `l2bmr2<rrn_opcode>`, `l2bmi`, `l2bmdars`, `l2bmdarw` はいずれも PE メモリへの直接書き込みを行わないため、3.6.2 の書き込みマスク / ゼロフラッシュマスク適用対象外
- 出力マスク: `l2bmb`, `l2bmb2`, `l2bmd`, `l2bm@<l1badr>`, `l2bmr<rrn_opcode>`, `l2bmr2<rrn_opcode>`, `l2bmi`, `l2bmdars`, `l2bmdarw` はいずれもマスクフラグを生成しない

#### `l2bmb`

- 種別: L2BM
- 基本構文: `l2bmb[@<l1bset>] $lc<addr_c> $lb<addr_b>`
- 内容: L2BM から L1BM へ放送する
- 実装メモ: L2BM 側 16 長語単位。部分集合指定で対象 L1B を絞れる

#### `l2bmb2`

- 種別: L2BM
- 基本構文: `l2bmb2[@<l1bset>] $lc<addr_c> $lb<addr_b>`
- 内容: L2BM から L1BM への分配放送
- 実装メモ: L1B を 2 個単位で扱う再配置放送。通常放送より広い単位で L2BM を読む

#### `l2bmd`

- 種別: L2BM
- 基本構文
  - `l2bmd[@<l1bset>] $lc<addr_c> $lb<addr_b>`
  - `l2bmd $lb<addr_b> $lc<addr_c>`
- 内容
  - 前者: L2BM -> L1BM の分配
  - 後者: L1BM -> L2BM の結合
- 実装メモ: 同じニーモニックで方向が逆転する。実装ではオペランド種別で分岐するのが安全

#### `l2bm@<l1badr>`

- 種別: L2BM
- 基本構文: `l2bm@<l1badr> $lb<addr_b> $lc<addr_c>`
- 内容: 指定 L1B から L2BM へ個別転送する
- 実装メモ: `<l1badr>` は 0 から 7。L1B 選択がオペコードに埋め込まれる

#### `l2bmr<rrn_opcode>`

- 種別: L2BM
- 基本構文: `l2bmr<rrn_opcode>[@<l1bset>] $lb<addr_b> $lc<addr_c>`
- 内容: L1BM 群を縮約しながら L2BM に書き込む
- 実装メモ: `l2bmb` と対になる縮約版

#### `l2bmr2<rrn_opcode>`

- 種別: L2BM
- 基本構文: `l2bmr2<rrn_opcode> $lb<addr_b> $lc<addr_c>`
- 内容: 結合縮約を伴う L1BM -> L2BM 転送
- 実装メモ: `l2bmb2` の逆方向に相当する

#### `l2bmi`

- 種別: L2BM
- 基本構文: `l2bmi@<l1bset> $lb<addr_src> $lb<addr_dst>`
- 内容: L1BM 間の内部マルチキャスト
- 実装メモ: 読み出し元集合と書き込み先対応は `immode` によって決まる

#### `l2bmdars`

- 種別: L2BM
- 基本構文: `l2bmdars $lc<addr>@.<l2b> $dar<addr>`
- 内容: L2BM から DARBUF 経由で DAR にアドレス値を書き込む準備をする
- 実装メモ: `l2bmdarw` とセットで使う

#### `l2bmdarw`

- 種別: L2BM
- 基本構文: `l2bmdarw`
- 内容: DARBUF から DAR へアドレス値を書き込む
- 実装メモ: `l2bmdars` と同時発行可能。DAR 書き込みタイミングに注意

### 7.4 L1BM 命令

#### 共通補足

- L1BM 命令式一覧は `l1bmp`, `l1bmm`, `l1bmm@`, `l1bmr`, `l1bmm4`, `l1bmm4@`, `l1bmr4`, `l1bmd`
- 同一種別の `PE -> L1BM` と `L1BM -> PE` の間では `lbi` を使う折り返し動作が可能
- PE 側オペランド語長と命令の PE 側速度が一致しない場合、ゼロ埋めまたは切り捨てが起こる
- 単精度縮約では `e` による半精度入力拡張、`r` による半精度への書き戻しがある
- マスク
  - `l1bmp`, `l1bmm`, `l1bmm4`, および `l1bmd` の `L1BM -> PE` 方向では、各 `<dst...>` に単一行書き込みマスクを付けられる
  - 同じ `L1BM -> PE` 方向の命令群には、オペコード側へゼロフラッシュマスクも付けられる
  - `l1bmm@<mabadr>`, `l1bmr<rrn_opcode>`, `l1bmm4@<mabadr>`, `l1bmr4<rrn_opcode>`, および `l1bmd` の `PE -> L1BM` 方向は PE メモリを書き込み先に持たないため、書き込みマスク / ゼロフラッシュマスク適用対象外
- 出力マスク: `l1bmp`, `l1bmm`, `l1bmm@<mabadr>`, `l1bmr<rrn_opcode>`, `l1bmm4`, `l1bmm4@<mabadr>`, `l1bmr4<rrn_opcode>`, `l1bmd` はいずれもマスクフラグを生成しない

#### `l1bmp`

- 種別: L1BM
- 基本構文: `l1bmp $lb... <dst...>` または `l1bmp $llb... <dst...>`
- 内容: L1BM から全 64 PE に PE 放送する
- 実装メモ
  - 1 長語版と 2 長語版がある
  - 対称な `PE -> L1BM` の `l1bmp` は存在しない

#### `l1bmm`

- 種別: L1BM
- 基本構文
  - `l1bmm $lb... <dst...>`
  - `l1bmm $llb... <dst...>`
- 内容: 16x1 MAB モードで放送する
- 実装メモ: 16 個の MAB へ分配しながら放送する基本命令

#### `l1bmm@<mabadr>`

- 種別: L1BM
- 基本構文
  - `l1bmm@<mabadr> <src> $lb...`
  - `l1bmm@<mabadr> <src> $llb...`
- 内容: 16x1 MAB モードの個別転送。PE -> L1BM 方向
- 実装メモ: 折り返し転送の片側としても使う

#### `l1bmr<rrn_opcode>`

- 種別: L1BM
- 基本構文
  - `l1bmr<rrn_opcode> <src> $lb...`
  - `l1bmr<rrn_opcode> <src> $llb...`
- 内容: 16x1 MAB モードの縮約転送
- 実装メモ
  - 単精度縮約では `e`, `r` が重要
  - `l1bmrhfadd` は `l1bmrffaddr ...e` のエイリアス関係になる

#### `l1bmm4`

- 種別: L1BM
- 基本構文: `l1bmm4 $lb... <dst...>` または `l1bmm4 $llb... <dst...>`
- 内容: 4x4 MAB モードの放送
- 実装メモ: MAB 番号上位 2 bit を分配・下位 2 bit を放送に使う特殊レイアウト

#### `l1bmm4@<mabadr>`

- 種別: L1BM
- 基本構文: `l1bmm4@<mabadr> <src> $lb...` または `... $llb...`
- 内容: 4x4 MAB モードの個別転送
- 実装メモ: 4x4 再配置にしたがって PE から L1BM へ戻す

#### `l1bmr4<rrn_opcode>`

- 種別: L1BM
- 基本構文: `l1bmr4<rrn_opcode> <src> $lb...` または `... $llb...`
- 内容: 4x4 MAB モードの縮約転送
- 実装メモ: 16x1 版より L1BM 側帯域が 4 倍になる

#### `l1bmd`

- 種別: L1BM
- 基本構文
  - `l1bmd $lb... <dst...>`
  - `l1bmd <src> $lb...`
- 内容
  - 前者: L1BM -> PE の分配
  - 後者: PE -> L1BM の結合
- 実装メモ: 同一ニーモニックで方向が逆。PE 側は 64 PE 全体の再配置を伴う
- マスク: `l1bmd $lb... <dst...>` の方向では `<dst...>` ごとに書き込みマスク指定可能で、オペコード側のゼロフラッシュマスクも適用できる。`l1bmd <src> $lb...` の方向ではどちらも適用できない

### 7.5 MAU 命令

#### 共通補足

- 行列モード: `mfma`, `mmul`
- ベクトルモード: `vfma`, `vmul`, `vadd`, `vpassa`
- 精度プレフィクス: `d`, `f`, `g`, `h`
- 共通オプション
  - `-<src>`: 行列以外の入力の符号反転
  - `<src>e`: 1 段低い精度からの精度拡張
  - `<src>r`: 通常の半精度入力を要求するオペランドに限り、単精度から半精度への精度縮減
  - `<opcode>r`: 出力の精度縮減
- MAU が生成するマスクフラグは「出力の符号ビットを反転したもの」である
- マスク
  - `dmfma`, `dmmul`, `fmfma`, `fmmul`, `gmfma`, `gmmul`, `hmfma`, `hmmul`, `dvfma`, `dvmul`, `dvadd`, `dvpassa`, `fvfma`, `fvmul`, `fvadd`, `fvpassa`, `hvfma`, `hvmul`, `hvadd`, `hvpassa` はすべて、各 `<dst...>` に書き込みマスクを付けられる
  - 同じ MAU 演算命令群はすべて、オペコード側へゼロフラッシュマスクを付けられる
  - ゼロフラッシュマスクを適用しても、MAU が生成するマスクフラグ自体は元の演算結果に基づいて決まり、ゼロ化後の値では再計算されない
- 出力マスク: `dmfma`, `dmmul`, `fmfma`, `fmmul`, `gmfma`, `gmmul`, `hmfma`, `hmmul`, `dvfma`, `dvmul`, `dvadd`, `dvpassa`, `fvfma`, `fvmul`, `fvadd`, `fvpassa`, `hvfma`, `hvmul`, `hvadd`, `hvpassa` はすべて、各出力値の符号ビットを反転したマスクフラグを生成する

#### `dmfma`

- 種別: MAU 行列ベクトル積和
- 基本構文: `dmfma(u|d) $l(x|y) <src_x> <src_y> <dst...>`
- 内容: 4x4 のブロックフロート倍精度行列 `A` を使い、上半分または下半分 2 行ぶんの `Ax + y` を計算する
- 実装メモ: `u` は 0,1 行、`d` は 2,3 行を選ぶ。4 行全体を得るには通常 2 命令必要

#### `dmmul`

- 種別: MAU 行列ベクトル積
- 基本構文: `dmmul(u|d) $l(x|y) <src_x> <dst...>`
- 内容: `dmfma` の加算入力を 0 にした倍精度行列ベクトル積
- 実装メモ: `mauf` を使って 2 命令で 4 行ぶんを繋ぐのが基本パターン

#### `fmfma`

- 種別: MAU 行列ベクトル積和
- 基本構文: `fmfma $l(x|y) <src_x> <src_y> <dst...>`
- 内容: 8x8 単精度行列から偶数列を使う 8x4 matvec FMA
- 実装メモ: `src_x` はブロックフロート単精度、`src_y` と出力は通常単精度

#### `fmmul`

- 種別: MAU 行列ベクトル積
- 基本構文: `fmmul $l(x|y) <src_x> <dst...>`
- 内容: `fmfma` の加算入力を 0 にしたもの
- 実装メモ: 出力は通常単精度、PE メモリへ長語書き込み

#### `gmfma`

- 種別: MAU 行列ベクトル積和
- 基本構文: `gmfma $l(x|y) <src_x> <src_y> <dst...>`
- 内容: 8x8 疑似単精度行列ベクトル積和
- 実装メモ: 行列レジスタ read/write 観点では `fmwrite` / `fmread` と同じ物理表現を使う

#### `gmmul`

- 種別: MAU 行列ベクトル積
- 基本構文: `gmmul $l(x|y) <src_x> <dst...>`
- 内容: `gmfma` の加算入力を 0 にしたもの
- 実装メモ: 出力は通常単精度

#### `hmfma`

- 種別: MAU 行列ベクトル積和
- 基本構文: `hmfma $l(x|y) <src_x> <src_y> <dst...>`
- 内容: 16x16 半精度行列ベクトル積和
- 実装メモ: 積は半精度、加算入力と出力は単精度。`src_y` と出力は 2 長語アクセス

#### `hmmul`

- 種別: MAU 行列ベクトル積
- 基本構文: `hmmul $l(x|y) <src_x> <dst...>`
- 内容: `hmfma` の加算入力を 0 にしたもの
- 実装メモ: 結果は単精度アキュムレーション結果

#### `dvfma`

- 種別: MAU ベクトル積和
- 基本構文: `dvfma(u|d) <src_x> <src_y> <src_z> <dst...>`
- 内容: 倍精度ベクトル `x * y + z`
- 実装メモ: `u` は PE0/1、`d` は PE2/3 側で積を計算する。全体積を得るには通常 2 命令要る

#### `dvmul`

- 種別: MAU ベクトル積
- 基本構文: `dvmul(u|d) <src_x> <src_y> <dst...>`
- 内容: `dvfma` の加算入力を 0 にしたもの
- 実装メモ: `mauf` と組み合わせる使い方が基本

#### `dvadd`

- 種別: MAU ベクトル加算
- 基本構文: `dvadd <src_x> <src_y> <dst...>`
- 内容: 倍精度ベクトル和 `x + y`
- 実装メモ: 各 PE 要素ごとの通常倍精度加算

#### `dvpassa`

- 種別: MAU ベクトルコピー
- 基本構文: `dvpassa <src_x> <dst...>`
- 内容: 倍精度ベクトルを素通しする
- 実装メモ: 浮動小数点数としての正規化が入るため完全ビットコピーではない

#### `fvfma`

- 種別: MAU ベクトル積和
- 基本構文: `fvfma <src_x> <src_y> <src_z> <dst...>`
- 内容: 単精度ベクトル `x * y + z`
- 実装メモ: 入出力は長語で、長語内 2 要素を並列演算する

#### `fvmul`

- 種別: MAU ベクトル積
- 基本構文: `fvmul <src_x> <src_y> <dst...>`
- 内容: `fvfma` の加算入力を 0 にしたもの
- 実装メモ: 通常単精度出力

#### `fvadd`

- 種別: MAU ベクトル加算
- 基本構文: `fvadd <src_x> <src_y> <dst...>`
- 内容: 単精度ベクトル和
- 実装メモ: `fvfma` の第 2 入力を 1 と見なす派生形

#### `fvpassa`

- 種別: MAU ベクトルコピー
- 基本構文: `fvpassa <src_x> <dst...>`
- 内容: 単精度ベクトルのコピー相当
- 実装メモ: 正規化が入るので厳密ビットコピーではない

#### `hvfma`

- 種別: MAU ベクトル積和
- 基本構文: `hvfma <src_x> <src_y> <src_z> <dst...>`
- 内容: 半精度ベクトル積和
- 実装メモ: `src_x`, `src_y` は半精度長語、`src_z` と出力は単精度 2 長語

#### `hvmul`

- 種別: MAU ベクトル積
- 基本構文: `hvmul <src_x> <src_y> <dst...>`
- 内容: `hvfma` の加算入力を 0 にしたもの
- 実装メモ: 単精度アキュムレーション結果を返す

#### `hvadd`

- 種別: MAU ベクトル加算
- 基本構文: `hvadd <src_x> <src_y> <dst...>`
- 内容: 半精度ベクトル和
- 実装メモ: `hvfma` の第 2 入力を 1 と見なす派生形

#### `hvpassa`

- 種別: MAU ベクトルコピー
- 基本構文: `hvpassa <src_x> <dst...>`
- 内容: 半精度ベクトルのコピー相当
- 実装メモ: 正規化が入るので厳密ビットコピーではない

### 7.6 行列レジスタ書き込み命令

#### 共通補足

- マスク: `dmwrite`, `fmwrite`, `gmwrite`, `hmwrite` は `mwrite` 系であり、行列レジスタを書き込み先に取るため、3.6.2 の書き込みマスク / ゼロフラッシュマスク適用対象外
- 出力マスク: `dmwrite`, `fmwrite`, `gmwrite`, `hmwrite` はいずれもマスクフラグを生成しない

#### `dmwrite`

- 種別: 行列書き込み
- 基本構文: `dmwrite <src> $l(x|y)<addr>`
- 内容: 倍精度データを行列レジスタへ書く
- 実装メモ
  - MAU 用ならブロックフロート倍精度を書き込む
  - 転置読み出し用なら通常倍精度ビット列でもよい
  - 4x4 全体を 1 命令で埋められる

#### `fmwrite`

- 種別: 行列書き込み
- 基本構文: `fmwrite <src> $l(x|y)<addr>`
- 内容: 単精度データを書き込む
- 実装メモ: `gmwrite` と物理動作は同一で、同時発行する MAU 精度に合わせて名前を使い分ける

#### `gmwrite`

- 種別: 行列書き込み
- 基本構文: `gmwrite <src> $l(x|y)<addr>`
- 内容: 疑似単精度データを書き込む
- 実装メモ: `fmwrite` と同機能

#### `hmwrite`

- 種別: 行列書き込み
- 基本構文: `hmwrite <src> $(l|ll)(x|y)<addr>`
- 内容: 半精度データを書き込む
- 実装メモ
  - `l` は 1 行/サイクル、`ll` は 2 行/サイクル
  - `ll` のとき `<addr>` は偶数アライン必須

### 7.7 行列レジスタ転置読み出し命令

#### 共通補足

- マスク: `dmread`, `fmread`, `gmread`, `hmread` は `mread` 系であり、各 `<dst...>` に書き込みマスクを付けられる。また、オペコード側へゼロフラッシュマスクを付けて PE メモリへ渡す値をゼロ化できる
- 実装メモ: `mread` 自体は ALU / MAU のようなマスクフラグ生成器ではなく、3.6.2.2 のゼロフラッシュ対象に含まれる読み出し経路として扱う
- 出力マスク: `dmread`, `fmread`, `gmread`, `hmread` はいずれもマスクフラグを生成しない

#### `dmread`

- 種別: 行列読み出し
- 基本構文: `dmread $l(x|y)<addr> <dst...>`
- 内容: 倍精度行列を転置して PE 側へ読み出す
- 実装メモ: 出力はビット列コピーであり、整数行列の転置にも使える

#### `fmread`

- 種別: 行列読み出し
- 基本構文: `fmread $l(x|y)<addr> <dst...>`
- 内容: 単精度行列の転置読み出し
- 実装メモ: `gmread` と物理動作は同一

#### `gmread`

- 種別: 行列読み出し
- 基本構文: `gmread $l(x|y)<addr> <dst...>`
- 内容: 疑似単精度行列の転置読み出し
- 実装メモ: `fmread` と同機能

#### `hmread`

- 種別: 行列読み出し
- 基本構文: `hmread $ll(x|y)<addr> <dst...>`
- 内容: 半精度行列を 2 列ずつ転置読み出しする
- 実装メモ
  - 常に `ll` を使う
  - `<addr>` は偶数アライン必須

### 7.8 ALU 命令

#### 共通補足

ALU 一般構文は次のいずれかである。

```text
[u][(d|f|g|h|l|i|s)]<opcode> <src_x> [<src_y>] <dst_0> [<dst_1>..]
zero <dst_0> [<dst_1>..]
imm[u] <payload> <dst_0> [<dst_1>..]
```

- `u`: 符号なしオプション。サポート有無は命令ごとに異なる
- 精度指定
  - `int`: `l`, `i`, `s`
  - `float`: `d`, `f`, `h`
  - `both`: `d`, `f`, `h`, `l`, `i`, `s`
  - `bfn`: `d`, `f`, `g`, `h`
  - `bfe`: `h` のみ
  - `untyped`: 精度指定なし
- `2` 長語動作しない命令では、出力の LSB 側 1 長語は第 1 入力の LSB 側 1 長語がそのまま残る
- 半精度浮動小数点数を期待する入力には、オペランド末尾の `r` で単精度から半精度への精度縮減を指示できる
- この縮減は入力ポートごとに独立に起こり、`2` 長語入力を `4` 個の単精度として丸め、MSB 側 1 長語へ `4` 個の半精度を詰め、LSB 側 1 長語は `0` になる
- すべての ALU 命令は、各 `<dst...>` に単一行書き込みマスクを付けられる。出力先がマスクレジスタであっても同様である
- すべての ALU 命令は、オペコード側へゼロフラッシュマスクを付けられる
- ALU のマスクフラグは常に 1 サイクルあたり `4` bit 出力され、長語精度では 1 語の結果が `4` bit に、単語精度では 2 語の結果がそれぞれ `2` bit ずつに複製される
- `zero`, `imm`, `msl`, `msr`, `floor`, `ftoi`, `bfe`, `bfn` は ALU マスクフラグを立てない
- `passa` のマスクフラグ判定では LSB 側 1 長語は無視される
- `inc` / `dec` / `add` / `sub` は、符号ありでは結果が非負、符号なしではオーバーフローなしのときフラグ `1`
- `not` / `lnot` / `and` / `or` / `xor` / `lsl` / `lsr` / `bsl` / `bsr` は、出力が all-0 のときフラグ `1`
- `rsqrt` / `relu` / `relu0` / `lrelud` / `lreluo` / `ilrelud` は `x` の MSB が `0` のときフラグ `1`、`relu1` / `relu2` / `relu3` はそれぞれ第 `2` / `3` / `4` MSB で判定する
- `max` / `min` は `x` が選ばれたとき、または `x == y` のときフラグ `1`、`packbit` は `y` の MSB が `0` のときフラグ `1`
- 出力マスクの命令別整理
  - `zero`, `imm`, `msl`, `msr`, `floor`, `ftoi`, `bfe`, `bfn`: マスクフラグを生成しない
  - `passa`: 出力が all-0 のとき `1`。判定では LSB 側 1 長語を無視する
  - `inc`, `dec`, `add`, `sub`: 符号ありでは結果が非負のとき `1`、符号なしではオーバーフローなしのとき `1`
  - `not`, `lnot`, `and`, `or`, `xor`, `lsl`, `lsr`, `bsl`, `bsr`: 出力が all-0 のとき `1`
  - `rsqrt`, `relu`, `relu0`, `lrelud`, `lreluo`, `ilrelud`: `src_x` の MSB が `0` のとき `1`
  - `relu1`: `src_x` の第 2 MSB が `0` のとき `1`
  - `relu2`: `src_x` の第 3 MSB が `0` のとき `1`
  - `relu3`: `src_x` の第 4 MSB が `0` のとき `1`
  - `max`, `min`: `src_x` が選ばれたとき、または `src_x == src_y` のとき `1`
  - `packbit`: `src_y` の MSB が `0` のとき `1`

#### `zero`

- 種別: ALU
- 基本構文: `zero <dst...>`
- 内容: 常に 2 長語の all-0 を出力する
- 実装メモ: 入力を取らない

#### `imm`

- 種別: ALU
- 基本構文: `imm[u] <payload> <dst...>`
- 内容: 即値ペイロードを 2 長語に展開して出力する
- 実装メモ
  - `u` なしは `xxxx`、`u` ありは `xoxo` 形に単語を並べる
  - LM0 アクセス命令と同時発行できない

#### `msl`

- 種別: ALU
- 基本構文: `msl <src> <dst...>`
- 内容: PE 間循環シフト。`0 -> 1 -> 2 -> 3 -> 0`
- 実装メモ: 精度指定なし。1 長語動作

#### `msr`

- 種別: ALU
- 基本構文: `msr <src> <dst...>`
- 内容: `msl` の逆方向の PE 間循環シフト
- 実装メモ: 精度指定なし。1 長語動作

#### `passa`

- 種別: ALU
- 基本構文: `<prec>passa <src> <dst...>`
- 内容: 入力 2 長語をそのまま出力するコピー命令
- 実装メモ: 生成マスクフラグは「出力が全 0 か」で決まる

#### `inc`

- 種別: ALU
- 基本構文: `[u]<int-prec>inc <src> <dst...>`
- 内容: 整数インクリメント
- 実装メモ: 符号ありでは非負判定、符号なしではオーバーフロー有無がマスクフラグになる

#### `dec`

- 種別: ALU
- 基本構文: `[u]<int-prec>dec <src> <dst...>`
- 内容: 整数デクリメント
- 実装メモ: `inc` と同系統

#### `not`

- 種別: ALU
- 基本構文: `<int-prec>not <src> <dst...>`
- 内容: ビット反転
- 実装メモ: 生成マスクフラグは「出力が all 0」

#### `lnot`

- 種別: ALU
- 基本構文: `<int-prec>lnot <src> <dst...>`
- 内容: 論理否定。入力が 0 なら 1、そうでなければ 0
- 実装メモ: `not` とは別物

#### `rsqrt`

- 種別: ALU
- 基本構文: `<float-prec>rsqrt <src> <dst...>`
- 内容: 近似逆数平方根
- 実装メモ: 入力の符号ビットは演算値には使われず、精度はおおむね 5 bit 程度。マスクフラグは `src` の符号に依存する

#### `floor`

- 種別: ALU
- 基本構文: `<float-prec>floor <src> <dst...>`
- 内容: floor 演算
- 実装メモ: `-inf` 方向へ丸める。無限大やゼロはそのまま通し、それ以外で結果がゼロなら仮数はゼロフラッシュされる。生成マスクフラグなし

#### `ftoi`

- 種別: ALU
- 基本構文: `<float-prec>ftoi <src> <dst...>`
- 内容: 浮動小数点を整数に変換する
- 実装メモ: ゼロ方向丸めで整数化する。符号なしモードでは入力の絶対値を符号なし整数へ変換し、無限大を含めて表現範囲を超えると最大値にクリップする。生成マスクフラグなし

#### `bfe`

- 種別: ALU
- 基本構文: `hbfe <src> <dst...>`
- 内容: half block float を extended half block float に変換する
- 実装メモ: `h` のみ有効。ブロックフロート化パイプラインの補助命令

#### `bfn`

- 種別: ALU
- 基本構文: `<d|f|g|h>bfn <src> <dst...>`
- 内容: ブロックフロート化を行う
- 実装メモ: MAU の行列入力を作る前段で多用する

#### `max`

- 種別: ALU
- 基本構文: `[u]<prec>max <src_x> <src_y> <dst...>`
- 内容: 最大値を返す
- 実装メモ: float と int の両方を持つ。整数は通常比較、浮動小数点は純粋な IEEE 754 max ではなく、両方がゼロなら `x` を優先し、同符号の無限大は仮数比較を行う。マスクフラグは `x` が選ばれたか、または `x == y`

#### `min`

- 種別: ALU
- 基本構文: `[u]<prec>min <src_x> <src_y> <dst...>`
- 内容: 最小値を返す
- 実装メモ: 整数は通常比較、浮動小数点は `max` と同系統の特殊比較規則を持つ。`max` と同様に、両方がゼロなら `x` を優先し、同符号の無限大は仮数比較を行う

#### `packbit`

- 種別: ALU
- 基本構文: `<prec>packbit <src_x> <src_y> <dst...>`
- 内容: 第 1 入力を左詰めし、第 2 入力の MSB を取り込む符号部パック命令
- 実装メモ: マスクフラグは `src_y` の MSB で決まる

#### `and`

- 種別: ALU
- 基本構文: `<int-prec>and <src_x> <src_y> <dst...>`
- 内容: ビット論理積
- 実装メモ: マスクフラグは出力が all 0 かどうか

#### `or`

- 種別: ALU
- 基本構文: `<int-prec>or <src_x> <src_y> <dst...>`
- 内容: ビット論理和
- 実装メモ: マスクフラグは出力が all 0 かどうか

#### `xor`

- 種別: ALU
- 基本構文: `<int-prec>xor <src_x> <src_y> <dst...>`
- 内容: 排他的論理和
- 実装メモ: マスクフラグは出力が all 0 かどうか

#### `add`

- 種別: ALU
- 基本構文: `[u]<int-prec>add <src_x> <src_y> <dst...>`
- 内容: 整数加算
- 実装メモ: 符号あり/なしでマスクフラグ条件が変わる

#### `sub`

- 種別: ALU
- 基本構文: `[u]<int-prec>sub <src_x> <src_y> <dst...>`
- 内容: 整数減算
- 実装メモ: マスクフラグ条件は `add` と同系統

#### `lsl`

- 種別: ALU
- 基本構文: `<int-prec>lsl <src_x> <src_y> <dst...>`
- 内容: 左シフト
- 実装メモ: マスクフラグは出力 all 0 判定

#### `lsr`

- 種別: ALU
- 基本構文: `[u]<int-prec>lsr <src_x> <src_y> <dst...>`
- 内容: 右シフト
- 実装メモ: 符号あり/なしの違いを持つ

#### `bsl`

- 種別: ALU
- 基本構文: `<int-prec>bsl <src_x> <src_y> <dst...>`
- 内容: 循環左シフト
- 実装メモ: barrel shift 系

#### `bsr`

- 種別: ALU
- 基本構文: `<int-prec>bsr <src_x> <src_y> <dst...>`
- 内容: 循環右シフト
- 実装メモ: barrel shift 系

#### `relu`

- 種別: ALU
- 基本構文: `<float-prec>relu <src_x> <src_y> <dst...>`
- 内容: `src_x` の符号で `src_y` を通すか `-0` にする
- 実装メモ: `src_x` が条件、`src_y` がデータ

#### `relu0`

- 種別: ALU
- 基本構文: `<float-prec>relu0 <src_x> <src_y> <dst...>`
- 内容: `relu` と同義
- 実装メモ: 名前違いの同等命令として扱ってよい

#### `relu1`

- 種別: ALU
- 基本構文: `<float-prec>relu1 <src_x> <src_y> <dst...>`
- 内容: `src_x` の第 2 MSB を条件に `src_y` を通す
- 実装メモ: マスクフラグ条件も第 2 MSB 準拠

#### `relu2`

- 種別: ALU
- 基本構文: `<float-prec>relu2 <src_x> <src_y> <dst...>`
- 内容: `src_x` の第 3 MSB を条件に `src_y` を通す
- 実装メモ: 条件ビット位置のみが `relu1` と異なる

#### `relu3`

- 種別: ALU
- 基本構文: `<float-prec>relu3 <src_x> <src_y> <dst...>`
- 内容: `src_x` の第 4 MSB を条件に `src_y` を通す
- 実装メモ: 条件ビット位置のみが `relu1` / `relu2` と異なる

#### `lrelud`

- 種別: ALU
- 基本構文: `<float-prec>lrelud <src_x> <src_y> <dst...>`
- 内容: `x >= 0` なら `y`、負なら `y / 2`
- 実装メモ: Leaky ReLU 系

#### `lreluo`

- 種別: ALU
- 基本構文: `<float-prec>lreluo <src_x> <src_y> <dst...>`
- 内容: `x >= 0` なら `y`、負なら `y / 8`
- 実装メモ: Leaky ReLU 系

#### `ilrelud`

- 種別: ALU
- 基本構文: `<float-prec>ilrelud <src_x> <src_y> <dst...>`
- 内容: `x >= 0` なら `y`、負なら `y` の指数を 1 段増やす系の変形 Leaky ReLU
- 実装メモ: 通常の算術除算ではなく指数操作として理解する

## 8. アセンブラ / インタプリタ実装で重要な論点

### 8.1 パーサが持つべき知識

- MV と PE でオペランド文法が異なる
- 同一ニーモニックでも方向が逆転するものがある
  - `l2bmd`
  - `l1bmd`
- L1BM / L2BM の部分集合指定や `@<mabadr>`, `@<l1badr>` はオペコード埋め込み情報として扱う必要がある
- MAU/ALU では接頭辞 `u`, 精度接頭辞, 接尾辞 `e`, `r`, 入力 `-` を正しく分離して解釈する必要がある

### 8.2 実行器が持つべき知識

- MV は 4 ステップに 1 回だけ進む
- T レジスタと flat mode はサイクルごとの値を持つ
- 行列レジスタは論理行数と物理行番号が精度でずれる
- マスクは「どの値を抑止するか」ではなく「どの粒度にどのビットを当てるか」を正しく実装する必要がある
- `wait` はストリーム全体の進行を止める
- PE ハザードは一律レイテンシではなく、L1BM/L2BM/PE メモリ間の組み合わせごとに待ちステップ数と同一アドレス条件が変わる
- PE の同時発行可否は opcode グループ、MAU の精度一致条件、行列レジスタの競合、`nop` の排他条件で決まる

### 8.3 数値モデルで見落としやすい点

- RRN の `fadd` は「指数合わせ前」「加算後」の両方で最近接偶数丸めを含む
- RRN の `max/min` は IEEE 754 的な順序ではなく、符号・絶対値表現の整数比較として扱う
- MAU の単精度・倍精度乗算は、完全な仮数積ではなく下位部分積を省略した近似乗算器である
- MAU の精度縮減は「基本精度に一度丸めてから縮減」ではなく、縮減後の精度へ直接丸める
- ブロックフロートは単なる shared exponent ではなく、無限大・ゼロ・半精度拡張表現に個別規則がある

### 8.4 省略せずに実装すべき検証

- アドレス上限チェック
- アラインメントチェック
- PE 命令のハザード待ちステップ数チェック
- PE 命令の並列実行条件チェック
- マスク適用語長とアクセス語長の整合性チェック
- `mreadf` の ALU 第 1 入力制約
- `nowrite` の単独出力制約
- `wait` の単独発行禁止
- `imm` と LM0 アクセス同時発行禁止
- MAU 入力の `r` が許されるオペランド種別のチェック
- ALU 入力の単精度 -> 半精度縮減の構文とデータ配置チェック

## 9. 第 4 章由来のビット正確意味論

### 9.1 適用範囲

manual 第 4 章は、MN-Core 2 の浮動小数点数演算について、実際の RRN および MAU とビットレベルで一致するエミュレータを実装するための仕様を与えている。manual が明示する例外は 1 つだけで、`拡張表現ありの半精度行列ベクトル積和演算` の厳密な乗算器内部シフトは簡略化されている。

実装上は次の 6 つを別物として持つとよい。

- 出力正規化
- RRN の `fadd`
- RRN の `max/min`
- MAU のベクトル積和
- MAU の行列ベクトル積和
- ブロックフロート化

### 9.2 出力正規化

RRN と MAU で「出力を正規化する」という語は同じ意味を持つ。

- 出力の指数部が全ビット 1 なら、仮数部を全ビット 0 にする
- 出力の指数部が全ビット 0 なら、仮数部を全ビット 0 にする
- さらにゼロ出力では符号ビットを 0 にする

これは IEEE 754 の一般的な正規化というより、MN-Core 2 の後段出力整形規則として扱うのが正確である。

### 9.3 RRN の加算縮約

#### 9.3.1 適用箇所

- MV 命令の縮約
- L2BM 命令の縮約
- L1BM 命令の縮約

L1BM には 4 要素縮約と 16 要素縮約があり、16 要素縮約は 4 要素縮約を 2 段行って実現する。MV の 8 要素縮約も 2 段構成で、中間値は正規な浮動小数点数として一度確定する。

#### 9.3.2 `fadd` のアルゴリズム

RRN の浮動小数点数加算縮約は、単なる逐次加算ではない。4 要素縮約については各入力要素ごとに次を行う。

1. 仮数部の MSB 側に hidden bit を補う
2. LSB 側に 3 ビットの 0 を補う
3. 最大指数を持つ要素に指数を合わせるため、他要素の仮数を右シフトする
4. 指数差が 4 以上で、補った 3 ビットをはみ出すときは、そのシフト時点で最近接偶数丸めを行う
5. そろえた仮数を加算縮約する
6. 出力精度に対して最近接偶数丸めを行う
7. 出力正規化を行う

実装上の重要点は次のとおりである。

- 丸めは 1 回ではなく、指数合わせ時と最終出力時の 2 箇所に存在する
- 最終丸めの繰り上がりにより、結果指数は「入力中の最大指数 + 2」よりさらに 1 大きくなりうる
- L1BM の 16 要素縮約でも「単精度に丸めてから半精度に丸め直す」のではなく、最終段で要求された精度に直接丸める

#### 9.3.3 半精度縮約の実体

L1BM 命令の半精度浮動小数点数縮約は、ハードウェアとしては単精度縮約回路で実装される。したがって L1BM の浮動小数点数縮約を厳密実装する場合、論理命令が半精度でも中間値の扱いは単精度回路準拠になる。

一方、MV 命令と L2BM 命令は半精度・単精度・倍精度の浮動小数点縮約回路を別に持つ。したがってインタプリタが「全部同じ縮約器」と仮定すると誤る。

### 9.4 RRN の最大値 / 最小値縮約

`max` / `min` 系の縮約は `fadd` と比較規則が異なる。

- 入力が無限大やゼロでも特別扱いしない
- 指数部と仮数部を分離せず、符号・絶対値表現の整数として比較する
- `+0` は `-0` より大きいとみなす
- `fadd` や MAU 出力と違い、結果の正規化は行わない

L1BM で精度縮減が指定されている場合だけ、最終結果に対して半精度への最近接偶数丸めが入る。16 要素縮約でも丸めは最終段の 1 回だけである。

### 9.5 MAU ベクトル積和のビット正確意味論

#### 9.5.1 基本形

出力 1 要素あたりの基本演算は `ab + c` である。`a`, `b` は hidden bit を持つ通常浮動小数点数として扱い、半精度では仮数積をそのまま計算する。

その後は全精度で共通して、概念上次の順に処理される。

1. 積 `ab` を計算する
2. `c` を指数差ぶんシフトして、仮数長無限とみなして加算する
3. 出力精度に最近接偶数丸めを行う
4. 指数が無限大域または 0 域に入ったら指数部を対応する値にする
5. 出力正規化を行う

#### 9.5.2 単精度・倍精度の近似乗算器

単精度と倍精度では、積 `ab` は完全な仮数積ではなく、下位部分積を省略した近似乗算である。

- 単精度: 仮数下位 5 bit 同士の乗算を省略する
- 倍精度: 仮数下位 16 bit 同士の乗算を省略する

省略された部分積がすべて 0 なら補正項 `r` は 0、そうでなければ次の定数を加える。

- 単精度: `r = 2^-38`
- 倍精度: `r = 2^-74`

つまり「省略領域に何か 1 があれば、その省略領域の最上位だけ 1 だった場合と同じ寄与に置き換える」近似である。manual が示すとおり、これは hidden bit 同士の積に比べてかなり下位で起きる。

- 単精度では出力仮数部より 15 bit 下
- 倍精度では出力仮数部より 22 bit 下

通常は誤差として表面化しにくいが、大きなキャンセルがあると厳密 IEEE 結果と異なる。manual 例の `fvfma` では、厳密値 `0x4a000004` に対し実機相当結果は `0x4a000010` になる。

#### 9.5.3 精度拡張と精度縮減

- 入力精度拡張は、先に高精度へ厳密変換してから基本動作を行う
- 出力精度縮減は、基本精度に一度丸めてから縮減するのではなく、縮減後の精度へ直接丸める

このため、MAU の `...r` 付き命令を software fallback で実装するときに「まず基本結果を出してから narrow cast する」と 1 ulp 以上ずれる可能性がある。

### 9.6 ブロックフロート形式とブロックフロート化

#### 9.6.1 ブロックサイズ

ブロックフロート形式は、ブロック内で指数部を共有する中間表現である。

- 半精度 BF: ブロックサイズ 16、変換前は半精度
- 疑似単精度 BF: ブロックサイズ 8、変換前は単精度
- 単精度 BF: ブロックサイズ 4、変換前は単精度
- 倍精度 BF: ブロックサイズ 4、変換前は倍精度

有効な BF 値では、半精度拡張表現を除き、ブロック内の指数部が一致していなければならない。不正な BF 値を MAU の積入力に入れた場合の動作は未定義である。

#### 9.6.2 解釈規則

- 疑似単精度・単精度・倍精度・半精度 BF は、基本的には変換前の通常浮動小数点数と同じ指数部長・仮数部長で解釈する
- 疑似単精度 BF だけは仮数部 LSB 側 5 bit を無視して 0 とみなす
- 仮数部が全 0 なら 0 を表現できる

#### 9.6.3 疑似単精度・単精度・倍精度 BF 化手順

`g/f/d bfn` 相当の BF 化は概ね次の手順になる。

1. ブロック内の最大指数を求める
2. 最大指数を持つ要素の仮数が all-1 なら、繰り上がりを見越して共通指数を `+1` する
3. 無限大・ゼロの例外処理を行う
4. 各要素について共通指数との差を取る
5. hidden bit を補い、指数差だけ右シフトし、最近接偶数丸めを行う
6. 丸め結果が 0 でなければ、符号は入力のまま、指数部は共通指数、仮数部は丸め結果とする
7. 丸め結果が 0 なら、符号は入力のまま、指数部は共通指数、仮数部は全 0 とする

例外処理は特に重要である。

- 共通指数が無限大以上なら、全出力を「入力符号 + 無限大 + 仮数 0」にする
- 個別入力の指数部が全 0 なら、その要素の出力は「入力符号 + 共通指数 + 仮数 0」にする
- 入力すべての指数部が全 0 なら、全出力を「入力符号 + 指数 0 + 仮数 0」にする

#### 9.6.4 半精度 BF 化と拡張表現

半精度 BF では、変換後の仮数部長を 6 から 9 で指定できる。`b = 9 - 変換後仮数部長` と置く。

拡張表現なしでは、次が通常 BF 化との違いである。

- 共通指数は「最大指数 + b」に基づいて決まる
- hidden bit 補完後の丸めも、その短くなる仮数長に対して行う

拡張表現ありでは、さらに次の規則がある。

- 指数部が共通指数ではなく全 0 の要素を許す
- その要素は「共通指数から 6 引いた指数」とみなして解釈する
- 変換時に指数差が `6 + b` 以上のとき、専用フラグを立てて拡張表現側へ落とす
- ただし指数差が `6 + b` で、かつ有効仮数が all-1 の場合はフラグを立てない
- フラグあり要素は `指数差 - 6` だけ右シフトして丸め、出力指数部を全 0 にする
- フラグなし要素は通常どおり共通指数を使う
- どちらの場合も、丸め結果が全 0 なら指数部も仮数部も全 0 にする

拡張表現は半精度 BF にしか存在しない。疑似単精度・単精度・倍精度 BF には存在しない。

### 9.7 MAU 行列ベクトル積和のビット正確意味論

#### 9.7.1 基本形

行列ベクトル積和は、出力 1 要素について

```text
(sum_{i=1..n} ai * bi) + c
```

を計算する。ここで `ai`, `bi` はブロックフロート値である。

- 半精度: ブロックサイズ 16、仮数部長 9
- 疑似単精度: ブロックサイズ 8、仮数部長 18
- 単精度: ブロックサイズ 4、仮数部長 23
- 倍精度: ブロックサイズ 4、仮数部長 52

#### 9.7.2 積の扱い

- 拡張表現なしの半精度: 各 `Pi` を厳密に計算する
- 疑似単精度: 各 `Pi` を厳密に計算する
- 単精度・倍精度: ベクトル積和と同じく、各 `Pi` ごとに下位部分積省略 + 補正項方式を使う

ただし行列積和では、`i` 方向の加算は途中丸めなしで、十分な桁数の符号付き整数加算として行われる。ここがベクトル積和の「各積のあとに c と加算して丸める」モデルとの大きな違いである。

#### 9.7.3 半精度拡張表現ありの場合

半精度で拡張表現を使う場合だけ、各 `i` の積 `Pi` に対し追加シフトが入る。

- `ai`, `bi` の片方だけが拡張表現なら `Pi` を 6 bit 右シフトして丸める
- 両方が拡張表現なら `Pi` を 12 bit 右シフトして丸める

manual はこの処理の乗算器内部に依存する厳密定義までは与えておらず、ここが chapter 4 の明示的な限界である。したがって、拡張表現あり半精度 matvec だけは「完全ビット一致対象外」と注記して実装するのが安全である。

#### 9.7.4 内積後の処理

内積を求めたあとの処理は、ベクトル積和と同じである。

- `c` を指数差に応じてそろえて加算する
- 縮減指定があれば縮減後精度へ直接最近接偶数丸めする
- 指数域を処理する
- 出力正規化する

### 9.8 実装指針

ビット正確エミュレータを目指すなら、IEEE 754 の汎用ライブラリ呼び出しだけで済ませず、少なくとも次の内部関数を分離実装した方がよい。

1. hidden bit 展開と指数比較
2. 最近接偶数丸め
3. RRN `fadd` 用の二段丸め縮約
4. RRN `max/min` 用の符号・絶対値比較
5. MAU 単精度・倍精度乗算器の部分積省略モデル
6. ブロックフロート化と半精度拡張表現遷移
7. 出力正規化

逆に、近似エミュレータで十分なら、次は省略候補になる。

- 単精度・倍精度 MAU の下位部分積省略
- RRN の途中丸め位置
- 半精度 BF 拡張表現

ただしこれらを省略した実装は、数値が近く見えてもビット一致は失う。

## 10. 最小の実装戦略

アセンブラを作る場合は、次の順で内部表現を作るのが現実的である。

1. 行単位で `control`, `mv`, `pe-vliw` を分類する
2. オペコードから `prefix`, `precision`, `mnemonic`, `suffix` を分離する
3. オペランドを memory space ごとの AST に落とす
4. 並列実行条件と静的エラーをチェックする
5. 命令カテゴリごとのエンコード器に渡す

インタプリタを作る場合は、さらに次を持つと扱いやすい。

1. `Board -> Group -> L2B -> L1B -> MAB -> PE` のメモリ木
2. 4 サイクルを明示する PE ステップ実行器
3. MV キューと `wait` 用タグ完了状態
4. マスクとフォワーディングのラッチ状態

## 11. 補足

この文書は Software Development Manual の内容を実装向けに再構成した要約である。今回、第 4 章の数値演算意味論も取り込み、manual が但し書きする `拡張表現ありの半精度行列ベクトル積和演算` を除いて、ビット一致エミュレータに必要な主要規則をこのファイルに含めた。機械語ビット配置そのものやホスト API まで再現する場合は、機械語仕様や周辺ドキュメントも併読するのが望ましい。
